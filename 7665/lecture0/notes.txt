to construct the e1 vector:
[i == 1 ? 1.0 : 0.0 for i in 1:n]


using GraphViz

function
isposdef(A)
(they do a Cholesky, which is great)

@views 
	enable to write A[1:n,1:m] .+= B[1:n,1:k] * C[1:k,1:m]
        but this works on views of A, B and C!!!!

@bounding
	to remove the bound checking from julia


